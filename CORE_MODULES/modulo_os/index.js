const os = require('node:os')

// são funções, então precisam esta acompanhados de ()
 console.log(os.cpus()) // isso é um array, ou seja pode passar o indice [0].model

// console.log(os.freemen())
// console.log(os.homedir())
// console.log(os.type())
